services:
  - type: web
    name: wt-team-stats
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_team_sim:app.server
    autoDeploy: true